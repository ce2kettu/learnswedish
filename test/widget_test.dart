import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:learnswedish/main.dart';

void main() {
  testWidgets('...', (WidgetTester tester) async {
    await tester.pumpWidget(LearningSwedishApp());
  });
}
